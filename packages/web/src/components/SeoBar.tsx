export function SeoBar() {
  return (
    <div className="seo-bar" role="region" aria-label="Product description">
      <p>
        Write, test and benchmark <strong>grok</strong> and{" "}
        <strong>Logstash</strong> patterns in your browser — with field-type
        inference, ReDoS warnings, and exports for Vector, Fluent Bit, and
        OpenSearch.
      </p>
    </div>
  );
}
